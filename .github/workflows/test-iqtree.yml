name: Test GitHub Actions

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: |
          echo "Hello from GitHub Actions!"
          echo "The computational runner is working."
          echo
          echo "CPU:"
          nproc
          echo
          echo "Operating system:"
          uname -a
